<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">

  {% for url in static_urls %}
  <url>
    <loc>{{ url.loc }}</loc>
    <changefreq>daily</changefreq>
    <priority>0.8</priority>
  </url>
  {% endfor %}

  {% for movie in movies %}
  <url>
    <loc>{{ url_for('main.detail', det='movie', name=movie.slug, id=movie.id, _external=True) }}</loc>
    <lastmod>{{ movie.date_added.strftime('%Y-%m-%d') if movie.date_added else '2025-01-01' }}</lastmod>
    <changefreq>weekly</changefreq>
    <priority>1.0</priority>
  </url>
  {% endfor %}

  {% for item in series_list %}
  <url>
    <loc>{{ url_for('main.detail', det='series', name=item.slug, id=item.id, season=1, episode=1, _external=True) }}</loc>
    <lastmod>{{ item.date_added.strftime('%Y-%m-%d') if item.date_added else '2025-01-01' }}</lastmod>
    <changefreq>weekly</changefreq>
    <priority>0.9</priority>
  </url>
  {% endfor %}

  {% for trailer in trailers %}
  <url>
    <loc>{{ url_for('main.detail', det='trailer_watch', name=trailer.slug, id=trailer.id, _external=True) }}</loc>
    <changefreq>monthly</changefreq>
    <priority>0.6</priority>
  </url>
  {% endfor %}

</urlset>